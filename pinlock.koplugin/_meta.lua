local _ = require("gettext")

return {
    name = "pinlock",
    fullname = _("PinLock"),
    description = _([[PinLock allows you to secure your device with a 4-digit PIN. 
You can lock the screen manually from the menu or have it automatically lock when the device wakes up.]]),
}
